# Reuse the exact pinned model/spaCy stack from the dashboard, plus a tiny web
# layer. Plain FastAPI + uvicorn (the pattern already used by the droplet's
# other services) — no Streamlit runtime, which is where the SIGSEGV lived.
-r ../requirements.txt
fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9

# Vocab-list demo (/vocab). Kept out of the parent requirements.txt since it's
# specific to this FastAPI app, not the shared base layer. 0.3.2 is the first
# PyPI release carrying the doc-level build_vocab_list API this demo uses;
# publish it before deploying, or the install will 404.
latincy-vocab==0.3.2
